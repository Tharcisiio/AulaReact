export default function Exercicio4 ({baset,alturat})
{
let base = Number(baset)
let altura = Number(alturat)
let area = base * altura;

return(

    <div>
        Á area desse triangulo {area}
    </div>
)
}
